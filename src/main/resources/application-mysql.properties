# database init, supports mysql too
database=mysql
spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost/lab4}
spring.datasource.username=${MYSQL_USER:lab4}
spring.datasource.password=${MYSQL_PASS:lab4}
# SQL is written to be idempotent so this is safe
spring.sql.init.mode=always